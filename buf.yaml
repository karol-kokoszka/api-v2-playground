version: v1
name: buf.build/scylladb/protos
deps:
  - buf.build/googleapis/googleapis
  - buf.build/grpc-ecosystem/grpc-gateway
  - buf.build/envoyproxy/protoc-gen-validate
build:
  excludes:
    - vendor
lint:
  use:
    - DEFAULT
  ignore_only:
    RPC_REQUEST_RESPONSE_UNIQUE:
      - proto